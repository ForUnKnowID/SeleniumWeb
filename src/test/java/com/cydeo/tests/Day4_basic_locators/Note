Locators in the selenium

1 id -- id is unique,might be dynamic

2 name -- if we cannot use id, or it is dynamic we look for the name.
          <The radio button might be have duplicate name>


3 tagName-- When to use tagname
           Only there is one or the element i use is first in that body i will use tagname


4 className


5 linkText  – locates the links based on the exact match of the link's text

6 PartialLinkText - locates links based on the partial text match of the link's text.





7 xPath


8 CSS

div tag
id attuibute
radius = value
